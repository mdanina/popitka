# Отчет диагностики ошибки "iteration over a 0-d tensor" при диаризации

## Дата диагностики
2025-11-20

## Выполненные проверки

### 1. Версии библиотек ✅

**Результаты:**
- **NeMo Toolkit**: 2.5.3
- **PyTorch**: 2.5.1+cu121 (CUDA 12.1)
- **OmegaConf**: 2.3.0
- **Python**: 3.12.12

**Статус**: Версии установлены и совместимы. NeMo 2.5.3 - актуальная версия.

### 2. Доступность устройств ✅

**Результаты:**
- **CUDA доступна**: Да
- **CUDA версия**: 12.1
- **Устройство по умолчанию**: cuda

**Статус**: GPU доступен и настроен корректно.

### 3. Различия в конфигурации между файлами ⚠️

**Обнаружено критическое различие:**

#### ui.py (строки 127-178):
```python
config = OmegaConf.create({
    "device": device,
    "num_workers": 0,           # ← ЕСТЬ
    "sample_rate": 16000,        # ← ЕСТЬ
    "verbose": False,            # ← ЕСТЬ
    "diarizer": { ... }
})
OmegaConf.set_struct(config, False)  # ← ЕСТЬ
```

#### transcriber.py (строки 69-117):
```python
config = OmegaConf.create({
    "device": device,
    # num_workers: ОТСУТСТВУЕТ
    # sample_rate: ОТСУТСТВУЕТ
    # verbose: ОТСУТСТВУЕТ
    "diarizer": { ... }
})
# OmegaConf.set_struct: ОТСУТСТВУЕТ
```

**Гипотеза**: Параметры `num_workers`, `sample_rate` и `verbose` на верхнем уровне конфига могут вызывать проблемы в NeMo 2.5.3, если они не поддерживаются или обрабатываются некорректно.

### 4. Структура manifest файла ✅

**Проверено:**
```json
{
  "audio_filepath": "test.wav",
  "offset": 0,
  "duration": null,
  "label": "infer",
  "text": "-",
  "num_speakers": null,
  "rttm_filepath": null,
  "uem_filepath": null
}
```

**Статус**: Структура корректна, соответствует требованиям NeMo.

### 5. Сигнатура ClusteringDiarizer ✅

**Результат:**
```python
ClusteringDiarizer(self, cfg: Union[omegaconf.dictconfig.DictConfig, Any], speaker_model=None)
```

**Статус**: Конфигурация передается корректно.

### 6. Логи ошибок

**Файл nemo_error.txt содержит:**
- Ошибка UnicodeEncodeError при предзагрузке моделей (строка 10 в ui.py)
- НЕ содержит ошибку "iteration over a 0-d tensor"

**Вывод**: Ошибка "iteration over a 0-d tensor" возникает при реальной диаризации, а не при предзагрузке.

## Выявленные проблемы и гипотезы

### 🔴 КРИТИЧЕСКАЯ ГИПОТЕЗА #1: Неподдерживаемые параметры конфигурации

**Проблема**: В `ui.py` используются параметры `num_workers`, `sample_rate` и `verbose` на верхнем уровне конфига, которых нет в `transcriber.py`.

**Почему это может вызывать ошибку:**
1. NeMo может интерпретировать эти параметры неправильно
2. `sample_rate` на верхнем уровне может конфликтовать с автоматическим определением частоты
3. `num_workers: 0` может вызывать проблемы с DataLoader в некоторых версиях NeMo

**Способ проверки:**
- Сравнить с официальными примерами NeMo 2.5.3
- Попробовать убрать эти параметры из конфига в ui.py
- Проверить, поддерживает ли ClusteringDiarizer эти параметры на верхнем уровне

### 🟡 ГИПОТЕЗА #2: Проблема с OmegaConf.set_struct

**Проблема**: В `ui.py` используется `OmegaConf.set_struct(config, False)`, в `transcriber.py` - нет.

**Почему это может вызывать ошибку:**
- `set_struct(False)` позволяет NeMo добавлять параметры, но может привести к некорректной структуре конфига
- Может вызвать проблемы при доступе к вложенным параметрам

**Способ проверки:**
- Проверить, нужен ли `set_struct(False)` для NeMo 2.5.3
- Попробовать убрать эту строку и посмотреть, работает ли

### 🟡 ГИПОТЕЗА #3: Проблема с форматом аудио

**Проблема**: Неизвестны параметры аудиофайла, который вызывает ошибку.

**Способ проверки:**
```bash
# Проверить длительность
ffprobe -i <файл> -show_entries format=duration

# Проверить частоту дискретизации
ffprobe -i <файл> -show_entries stream=sample_rate

# Проверить каналы
ffprobe -i <файл> -show_entries stream=channels
```

### 🟡 ГИПОТЕЗА #4: Пустые результаты VAD

**Проблема**: VAD может не находить речевые сегменты, возвращая пустой результат, что приводит к попытке итерации по скаляру.

**Способ проверки:**
- Включить `verbose: True` в конфиге
- Проверить промежуточные файлы в `output_dir` после запуска
- Проверить наличие файлов VAD результатов

### 🟡 ГИПОТЕЗА #5: Проблема с speaker embeddings

**Проблема**: Модель `titanet_large` может возвращать скаляр вместо вектора embeddings для коротких или пустых сегментов.

**Способ проверки:**
- Проверить размерность выходов модели embeddings
- Попробовать другую модель embeddings
- Проверить логи на наличие предупреждений о размерах тензоров

### 🟡 ГИПОТЕЗА #6: Проблема с кластеризацией

**Проблема**: Кластеризация получает пустые или некорректные embeddings и пытается итерироваться по скаляру.

**Способ проверки:**
- Установить `oracle_num_speakers: True` и задать конкретное число спикеров
- Изменить параметры кластеризации
- Проверить количество найденных спикеров

## Рекомендуемый порядок проверки

### Приоритет 1 (Критично):
1. ✅ **Убрать `num_workers`, `sample_rate`, `verbose` из верхнего уровня конфига в ui.py**
   - Эти параметры могут не поддерживаться на верхнем уровне
   - Сравнить с конфигурацией в transcriber.py

2. ✅ **Проверить необходимость `OmegaConf.set_struct(config, False)`**
   - Может вызывать проблемы с доступом к параметрам
   - Попробовать убрать и посмотреть результат

### Приоритет 2 (Важно):
3. **Проверить формат аудиофайла**
   - Длительность, частота дискретизации, каналы
   - Конвертировать в WAV 16kHz моно при необходимости

4. **Включить verbose режим**
   - Добавить `verbose: True` в конфиг для детальных логов
   - Проверить, на каком этапе возникает ошибка

5. **Проверить результаты VAD**
   - Убедиться, что VAD находит речевые сегменты
   - Проверить промежуточные файлы в output_dir

### Приоритет 3 (Дополнительно):
6. **Проверить версии библиотек** (уже выполнено ✅)
7. **Проверить device** (уже выполнено ✅)
8. **Попробовать минимальную конфигурацию**

## Следующие шаги

1. **Сравнить конфигурацию с официальными примерами NeMo 2.5.3**
2. **Проверить аудиофайл**, который вызывает ошибку
3. **Включить детальное логирование** для определения точного места ошибки
4. **Попробовать упрощенную конфигурацию** без дополнительных параметров

## Дополнительная информация

- **Файл с ошибкой**: ui.py (строка 184: `sd_model.diarize()`)
- **Обработка ошибок**: В ui.py ошибка перехватывается на строке 306-319
- **Различия**: ui.py имеет дополнительные параметры конфига, которых нет в transcriber.py

## Выводы

**Наиболее вероятная причина**: Использование параметров `num_workers`, `sample_rate` и `verbose` на верхнем уровне конфигурации в ui.py, которые могут не поддерживаться или обрабатываться некорректно в NeMo 2.5.3, что приводит к созданию 0-мерных тензоров при обработке.

**Рекомендация**: Убрать эти параметры из верхнего уровня конфига и использовать конфигурацию, аналогичную transcriber.py, которая не содержит этих параметров.

